
	// Set the {{relatedEntityName|lower}}Ids from the comma separated string of {{relatedEntityName|lower}}-ids
	if ($return && array_key_exists('{{relatedEntityName|lower}}_ids', $array)) {
		$this->{{relatedEntityName|lower}}_ids = $array['{{relatedEntityName|lower}}_ids'];
	}
